Place local Soundfont JS files here for priority loading.

Expected naming pattern:
- `<instrument>-MusyngKite.js`

Example:
- `acoustic_grand_piano-MusyngKite.js`

When not found, the app automatically falls back to remote Soundfont and then to synth mode.
